package com.stockgame.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS는 SecurityConfig.corsConfigurationSource()에서 단일 관리합니다.
}
